{{- if .Values.aladin.configYamlContent }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "aladin.fullname" $ }}-config
data:
  aladin.yaml: |
{{ .Values.aladin.configYamlContent | toYaml | indent 4 }}

{{- end }}