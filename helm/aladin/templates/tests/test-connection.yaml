apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aladin.fullname" . }}-test-connection"
  labels:
    {{- include "aladin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aladin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
